// i - index
// for (let i = 0; i <= 10; i++) {
//     const par = i % 2 === 0 ? `${i} par` : `${i} ímpar`;
//     console.log(par);
// }

const frutas = ['Maça', 'Uva', 'Banana', 'Figo', 'Tomate', 'Manga'];

for (let i = 0; i < frutas.length; i++) {
    console.log(`Índice ${i} -`, frutas[i]);
}