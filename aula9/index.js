// String, Number, Undefined, Null, Boolean
const nome1 = 'Silva'; // string
const nome2 = "Silva"; // string
const nome3 = `Silva`; // string
const num1 = 2; // number
const num2 = 2.54; // number
let nomeCliente; // undefined -> não aponta pra local nenhum na memória
const sobrenomeCliente = null; // Nulo -> não aponta pra local nenhum na memória
const aprovado = false; // Boolean = true, false (lógico)

console.log(typeof aprovado, aprovado); // typeof -> retorna o tipo de dado 